version: "2"

linters:
  default: standard
  # errcheck disabled: the codebase intentionally ignores errors on
  # best-effort paths (deferred rollbacks, SSE writes to dying clients,
  # CSV flush); meaningful errors are checked and wrapped explicitly.
  disable:
    - errcheck
